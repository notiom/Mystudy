#ifndef _SORT_H
#define _SORT_H

#include <vector>

void sortSelect(std::vector<int> &nums);

void sortInsert(std::vector<int> &nums);

void sortBubble(std::vector<int> &nums);

void quickSort(std::vector<int>& arr, int low, int high);
#endif